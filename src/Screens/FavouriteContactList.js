import React, { useState, useEffect } from 'react';
import {
View,
Text,
StyleSheet,
SafeAreaView,
StatusBar,
Keyboard,
BackHandler,
FlatList,
Alert,
ActivityIndicator,
Image,
TouchableOpacity
} from 'react-native';

import {
Container, Header, Title, Left, Icon,
Right, Button, Body, Content, Card, CardItem } from "native-base";

import {
  Colors,
} from 'react-native/Libraries/NewAppScreen';


const FavouriteListScreen = () => {
};

export default FavouriteListScreen;